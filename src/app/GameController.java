package app;

import javax.swing.JPanel;

/**
 * 
 * should handle are movements of the main player and inputs along with what inputs do not work
 * should not store level or draw anything.
 * 
 */

// not used currently


public class GameController extends JPanel{
/*	private DrawingComponent drawing;
	
	public GameController() {
		setLayout(new BorderLayout());
		drawing = new DrawingComponent();
		add(drawing, BorderLayout.CENTER);
		
		SwingUtilities.invokeLater(() -> drawing.requestFocusInWindow());
	}
	
	from GamePrep, might be helpful for aj
 * 	private class ButtonListener implements ActionListener {
		@Override
		public void actionPerformed(ActionEvent e) {
			String buttonLabel = e.getActionCommand();
      if (buttonLabel.equals("Left")) {
        drawing.moveLeft();
      } else if (buttonLabel.equals("Right")) {
        drawing.moveRight();
      } else {
        drawing.reset();
      }
      // After handling a button logic in Controller (button Listener class):
   		drawing.requestFocusInWindow();
		}
		
	}*/

}
